export interface PostMetadata{
    title: string,
    description: string,
    slug: string
}

export interface PreviewMetadata{ 
    slug: string
}
export interface image{ 
    src: string,
    alt: string,
    title: string,
    width: string,
    height: string, 
}
