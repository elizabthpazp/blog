export interface PostMetadata{
    title: string,
    subtitle: string,
    description: string,
    slug: string
    date: string
    image: string
}

export interface PreviewMetadata{ 
    slug: string
}
export interface image{ 
    src: string,
    alt: string,
    title: string,
    width: string,
    height: string, 
}
